<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="comhala.halawyat">

    <uses-sdk tools:overrideLibrary="in.mayanknagwanshi.imagepicker" />
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ui.App"
        android:allowBackup="true"
        android:icon="@drawable/logog"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@drawable/logog"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.MyProfile">

        </activity>
        <activity android:name=".ui.Notifications" />
        <activity android:name=".ui.SettingPage" />
        <activity android:name=".ui.ContactUs" />
        <activity android:name=".ui.OrderDetails" />
        <activity android:name=".ui.MyOrders" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".ui.MapPage"
            android:label="@string/title_activity_map_page" />
        <activity android:name=".ui.ChooseDelivery" />
        <activity android:name=".ui.CartPage" />
        <activity android:name=".ui.StoreDetail" />
        <activity
            android:name=".ui.homePage"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ui.Areas" />
        <activity android:name=".ui.Regions" />
        <activity android:name=".ui.ChangePassword" />
        <activity android:name=".ui.Forgetpassword" />
        <activity android:name=".ui.VerfyCode" />
        <activity android:name=".ui.SignUp" />
        <activity android:name=".ui.Login" >

        </activity>
        <activity android:name=".ui.ChooseLanguage" />
        <activity
            android:name=".ui.Splash"
            android:theme="@style/AppTheme1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.MainActivity"
            android:theme="@style/AppTheme" />
        <service android:name=".helper.MyFirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/logog" />
        <meta-data android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/colorPrimary" />
    </application>

</manifest>